trigger:
  branches:
    include:
      - main
      - develop

pool:
  vmImage: ubuntu-latest

steps:

- script: |
    echo "Azure DevOps pipeline connected successfully!"
  displayName: 'Verify Pipeline'